import React from 'react';
import PlayIcon from '../../../lib/icons/PlayIcon';
import {
  Background,
} from './styles';

export default function PlayButton() {
  return (
    <Background>
      <PlayIcon />
    </Background>
  );
}
